; Comments in scheme begin with ;

; to define variable we use the clause `define`
; to do evaluation we enclose in parentheses
; We say that the name identifies a variable whose value is the object.
(define size 2)
; we use prefix notation in lisp:
;    placing the operator to the le of the operands
(* 5 size)
